get_filename_component(OPERATORS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

if(NOT TARGET @PROJECT_NAME@::@PROJECT_NAME@)
    include("${OPERATORS_CMAKE_DIR}/@PROJECT_NAME@-targets.cmake")
endif()

set(OPERATORS_LIBRARIES @PROJECT_NAME@::@PROJECT_NAME@)
